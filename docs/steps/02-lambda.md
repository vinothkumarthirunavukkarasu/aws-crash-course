# Step 2 — Create one Lambda manually and test it

**Status:** ⬜ Pending

## Objective

Create a Lambda function in the AWS Console and invoke it.

## Prerequisites

- [ ] Step 1 complete — AWS CLI configured and authenticated

## Tasks

- [ ] Create a Lambda function in the AWS Console
- [ ] Write a simple handler function
- [ ] Test the Lambda function in the console
- [ ] Invoke the Lambda function via CLI

## Verification

- [ ] Lambda function exists in `us-east-1`
- [ ] Lambda function returns expected output

## Next Step

Proceed to [Step 3 — Create a Step Function that calls Lambda](03-step-functions.md).